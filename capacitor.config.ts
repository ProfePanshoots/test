import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.nbaapp.duoc',
  appName: 'NBAAPP',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
